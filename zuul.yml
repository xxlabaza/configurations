
server:
  port: 9002

security.basic.enabled: false

zuul:
  sensitiveHeaders: Cache-Control
  request:
    debug: true
  ignoredServices: '*'
  prefix: /api
  routes:
    popa:
      path: /popa/**
      sensitiveHeaders: Cache-Control

popa:
  dummy: green
