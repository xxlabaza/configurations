
server:
  port: 9091

security.basic.enabled: false

eureka:
  instance:
    metadataMap:
      container:
        color: blue

---
spring:
  profiles: zone1

eureka:
  instance:
    metadataMap:
      container:
        color: green

server:
  port: 9090

---
spring:
  profiles: zone2

eureka:
  instance:
    metadataMap:
      container:
        color: blue